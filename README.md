# 🌟 Auto-Refresh Quote App

A React mini-project that automatically fetches a new quote every 10 seconds using **useEffect**, **cleanup**, and **async API calls**.

### 🔧 Features
- Auto-refresh quote every 10s (interval + cleanup)
- Manual refresh button
- Loading state
- Timestamp of last refresh
- Clean and simple UI

### 🧠 Concepts Practiced
- `useEffect` with dependency array
- Cleanup function (`return () => clearInterval(...)`)
- API fetching with `async/await`
- Loading and error states
- Simple UI rendering logic

Perfect mini-project for a future frontend portfolio.

Demo:
https://codesandbox.io/p/sandbox/fc8rc6?file=%2Fsrc%2FApp.js%3A3%2C1
